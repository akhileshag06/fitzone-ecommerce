services:
  - type: web
    name: fitzone-frontend
    env: node
    buildCommand: cd E-com && npm install && npm run build
    startCommand: cd E-com && npm start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
